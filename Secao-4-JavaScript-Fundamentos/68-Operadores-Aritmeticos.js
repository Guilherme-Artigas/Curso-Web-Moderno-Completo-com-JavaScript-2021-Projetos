const [a, b, c, d] = [5, 12, 1, 26];

const soma = b + d; // Soma
const subt = a - b; // Subtração
const mult = c * d; // Multiplicação
const divi = d / a; // Divisão
const modu = d % 2; // Módulo (resto da divisão)

console.log(soma); // 38
console.log(subt); // -7
console.log(mult); // 26
console.log(divi); // 5.2
console.log(modu); // 0