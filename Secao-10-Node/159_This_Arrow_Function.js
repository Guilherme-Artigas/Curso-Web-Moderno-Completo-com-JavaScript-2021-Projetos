/**
 * Essa foi uma aula bem rapida para lembrar que o this dentro de arrow function
 * tem um comportamento diferente, ou seja, quando trabalhamos com o this em funções declaradas
 * com a sintaxe de function() {} o this dentro dela aponta para o objeto global.
 * 
 * usando a sintaxe de arrow function o this apontara para o module.exports
 */
